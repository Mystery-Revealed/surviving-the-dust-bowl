{
  "name": "dust-bowl",
  "version": "1.0.0",
  "private": true,
  "description": "Surviving the Dust Bowl — Unit 7 Texas History decision game (TEKS 7.7E, 7.9B, 7.9A). Guide a Panhandle family (Farm-Owner, Tenant, or Town) through the Dust Bowl years, with a mid-game stay-or-go branch, over a server-authoritative Socket.IO backend with a live Teacher Command Center.",
  "comment": "No npm workspaces: this drive is exFAT, which cannot host the symlinks/junctions workspace linking requires. server/ and client/ install independently; `npm install` here cascades via postinstall.",
  "scripts": {
    "postinstall": "npm install --prefix server --no-fund --no-audit && npm install --prefix client --no-fund --no-audit",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "test": "npm test --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
